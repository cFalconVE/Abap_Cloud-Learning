projection implementation in class ZBP_C_419_FLIGHT unique;
strict ( 2 );
extensible;
use draft;
use side effects;
define behavior for ZC_419_FLIGHT alias Vuelo
extensible
use etag
{

field ( readonly ) PlaneTypeID;

//  use create;
  use update;
//  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

}